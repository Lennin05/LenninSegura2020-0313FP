@model List<Inscripcion>

<h1 class="display-3">Inscripcion</h1>


<br />
<br />

<div class="btn btn-success"> <a asp-action="Create" asp-controller="Inscripcion"><i class="fa-solid fa-floppy-disk"></i>Agregar</a></div>
<br />
<br />
<table class="table">

    <thead>
        <tr>
            <th>Id</th>
            <th>Fecha de Inscripcion</th>
        </tr>
    </thead>

    <tbody>

        @foreach (var inscripcion in Model)
        {
            <tr>
                <td>@Html.DisplayFor(m => inscripcion.IdInscripcion)</td>
                <td>@Html.DisplayFor(m => inscripcion.Fecha_Inscripcion)</td>
                <td>@Html.DisplayFor(m => inscripcion.Fecha_Limite)</td>
                <td><a class="btn btn-primary" asp-action="Edit" asp-route-id="@inscripcion.IdInscripcion"><i class="fa-solid fa-pen-to-square"></i>Editar</a></td>
                <td><a class="btn btn-danger" asp-action="Delete" asp-route-id="@inscripcion.IdInscripcion"><i class="fa-solid fa-trash-can"></i>Eliminar</a></td>

            </tr>

        }

    </tbody>
</table>
